<?php

namespace App\Models\DataLaporan;

use Illuminate\Database\Eloquent\Model;

class GDR extends Model
{
    // Rumus GDR (Gross Death Rate)
    // GDR = (Jumlah Kematian (per-periode) / Jumlah Pasien Keluar (per-periode)) * 1000 
}
